function createDropdownOptions() {

  var dropdownMenu = $('vehicleDropdown');
  $.getJSON('vehicle.JSON');
  $('Vehicles').each(function(
    
  ));
};

// Fetch JSON.
// Each ID is an item.

// Show Manufacturer > Name into dropdown menu.
// Show Model > Name into next dropdown menu.
// Filter Page content 